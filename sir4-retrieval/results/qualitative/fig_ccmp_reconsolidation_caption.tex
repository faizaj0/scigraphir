\textbf{Cross-field retrieval from computer science to neuroscience.}
(a) Query summary and numbered seed excerpts.
(b,c) Top two simple seed-to-gold paths; shared OpenIE nodes are merged.
(d) CCMP on/off uses the same trained weights; final denotes fused rank for graph methods.
Path weights are gradient attributions, not probabilities.
$r^{-1}$: inverse relation. $g$: sender gate with CCMP on; other shown gates round to 1.00.
